# Build tooling scratch — the shipped bundle lives in ../dashboard/dist (committed).
node_modules/
icons-extracted.json
package-lock.json
# Screenshot-QA harness — dev-only, never part of the shipped plugin bundle.
demo/
